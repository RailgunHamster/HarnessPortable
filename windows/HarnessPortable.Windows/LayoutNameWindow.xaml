<Window x:Class="HarnessPortable.Windows.LayoutNameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="保存布局预设" Icon="/Assets/HarnessPortable.ico"
        Height="190" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#F9FAFB"
        FontFamily="Segoe UI, Microsoft YaHei UI">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="预设名称" FontWeight="SemiBold"/>
        <TextBox x:Name="NameBox" Grid.Row="1" Margin="0,8,0,0"
                 Style="{StaticResource ModernTextBox}"/>
        <TextBlock Grid.Row="2" Margin="0,8,0,0" Foreground="#6B7280" FontSize="12"
                   Text="保存当前所有分屏、标签顺序，以及每个标签对应的隧道或直连地址。密码不会保存。"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="取消" Width="88" Margin="0,0,8,0"
                    Style="{StaticResource ModernButton}" IsCancel="True"/>
            <Button x:Name="SaveButton" Content="保存" Width="88"
                    Style="{StaticResource ModernPrimaryButton}" IsDefault="True" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
